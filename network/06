# Get the complete path of the file that contains the IP address
# of the DNS server you’re using
# Search for system configuration folder of any Unix system, then for resolv.conf.
cat /etc/resolv.conf
